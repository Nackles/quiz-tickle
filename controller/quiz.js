// feed this bad girl a quiz array

module.exports = function populateQuiz(quizObject) {
    const quizDiv = quizObject.map(question => 
        // creates a quiz form
        console.log("this baby can fit an infinite void of " + question + "s")
        )
    return quizDiv;
    }
